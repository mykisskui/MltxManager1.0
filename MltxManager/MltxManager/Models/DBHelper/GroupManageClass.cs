using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace MltxManager.Models.DBHelper
{
    //管理商品组类
    public class GroupManageClass
    {
        //获取商品分组信息
        public ReturnMsg GetGroupInfo()
        {
            ReturnMsg rmsg = new ReturnMsg();

            return rmsg;
        }

        //新增商品分组信息
        public ReturnMsg CreateGroupInfo()
        {
            ReturnMsg rmsg = new ReturnMsg();

            return rmsg;
        }

        //编辑商品分组信息
        public ReturnMsg EditGroupInfo()
        {
            ReturnMsg rmsg = new ReturnMsg();

            return rmsg;
        }

        //删除商品分组信息
        public ReturnMsg DeleteGroupInfo()
        {
            ReturnMsg rmsg = new ReturnMsg();

            return rmsg;
        }
    }
}